import { ShoppingListUserFilterPipe } from './shopping-list-user-filter.pipe';

describe('ShoppingListUserFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShoppingListUserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
