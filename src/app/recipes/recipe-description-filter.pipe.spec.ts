import { RecipeDescriptionFilterPipe } from './recipe-description-filter.pipe';

describe('RecipeDescriptionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeDescriptionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
