import { RecipesUserFilterPipe } from './recipes-user-filter.pipe';

describe('RecipesUserFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipesUserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
